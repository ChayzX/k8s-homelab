apiVersion: apps/v1
kind: Deployment
metadata:
  name: jmusicbot
  namespace: jmusicbot
spec:
  # The independent Oracle environment is recovery capacity. Starting this
  # Deployment is an explicit promotion step after the home Discord writer has
  # been fenced and confirmed stopped.
  replicas: 0
