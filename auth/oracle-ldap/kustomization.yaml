apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../50-ldap-outpost.yaml
patches:
  - path: deployment-patch.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: ldap-outpost
